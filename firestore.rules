
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read song data.
    // Restrict write access to only authenticated users.
    match /songs/{songId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
